import { DropdownCheckPipe } from './dropdown-check.pipe';

describe('DropdownCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
